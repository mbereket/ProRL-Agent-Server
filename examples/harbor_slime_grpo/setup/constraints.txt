# uv override constraints for the Harbor Slime GRPO example.
# Exported as UV_OVERRIDE by launch_e2e.sh, so every uv invocation (including
# the editable installs and any re-resolution) honors them. Each line fixes a
# resolution that otherwise drifts to an incompatible version:
#
# nvidia-cublas : TE 2.14's cu13 core needs cublasLtGroupedMatrixLayoutInit,
#                 absent from the 13.1.x cuBLAS that torch 2.11+cu130 pins.
#                 torch runs on newer 13.x via minor-version compatibility.
# numpy         : slime v0.3.0 asserts numpy 1.x at init (its own Dockerfile
#                 installs numpy<2); the sglang tree resolves numpy 2.x.
# scipy         : scipy 1.18 declares numpy>=1.26 but uses numpy-2-only np.long.
# wandb         : wandb 0.29 removed wandb.util.generate_id, which slime calls.
nvidia-cublas==13.6.1.10
numpy<2
scipy==1.13.1
wandb==0.22.3
